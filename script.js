// Aguarda o carregamento do documento
document.addEventListener('DOMContentLoaded', () => {
    const botaoSites = document.getElementById('btn-sites');

    // Exemplo de interação: Log no console quando o botão for clicado
    botaoSites.addEventListener('click', (event) => {
        console.log('O usuário está sendo redirecionado para a página de Sites...');
    });
});
